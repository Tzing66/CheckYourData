# Single-service production build (frontend served by the same FastAPI app it calls) —
# empty base URL means requests go to the current origin, so this works regardless of
# the deployed domain with no rebuild needed.
VITE_API_BASE_URL=
